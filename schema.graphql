schema {
    query: Query
    mutation: Mutation
}
type Query {
    user(id: ID!): User
}
type Mutation {
    createUser(user: CreateUser): String
}
type User {
    id: ID
    email: String
}
input CreateUser {
    email: String!
    password: String!
}
